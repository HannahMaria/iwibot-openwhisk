{
  "name": "router",
  "description": "OpenWhisk action that passes received parameters to the Watson Assistant and maps the returned intent to an action",
  "license": "Apache-2.0",
  "version": "1.0.0",
  "main": "lib/Router.js",
  "scripts": {
    "test": "grunt nodeunit"
  },
  "dependencies": {
    "request": "^2.81.0",
    "watson-developer-cloud": "^3.12.0"
  },
  "devDependencies": {
    "chai": "^4.2.0"
  }
}
